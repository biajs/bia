import { JsHelper } from '../../code/JsHelper';

/**
 * No operation.
 * 
 * @return {JsFunction}
 */
export default new JsHelper({
    id: 'noop',
    name: 'noop',
    signature: [],
    content: [],
});