import { JsFunction } from '../../code/index';

/**
 * Create element.
 * 
 * @return {JsFunction}
 */
export const createElement = new JsFunction({
    id: 'createElement',
    content: [
        `// create element`,
    ],
});