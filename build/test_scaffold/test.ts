import { code, expect, render } from '../../utils';

describe.only('_NAME_', () => {
    it('_NAME_', function() {
        const vm = code(`
            <template>
                <div></div>
            </template>
        `, {
            data: {

            },
        });
        
        console.log(vm);
        // console.log(vm.$el.outerHTML);
    });
});